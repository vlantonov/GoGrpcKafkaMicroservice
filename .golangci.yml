issues:
  # Exclude generated protobuf code from all linters.
  exclude-dirs:
    - gen

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gofmt
    - revive

linters-settings:
  revive:
    rules:
      - name: exported
        disabled: false
